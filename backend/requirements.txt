# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Config & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.12
alembic==1.13.3

# --- Background jobs (wired up fully in Phase 5/6) ---
celery==5.4.0
redis==5.0.8

# --- Auth (wired up fully in Phase 2) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# --- File metadata extraction (Phase 3) ---
pypdf==5.0.1
python-docx==1.1.2
EbookLib==0.18

# --- Book parsing (Phase 4) ---
beautifulsoup4==4.12.3
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0

# --- S3 storage (only used when STORAGE_BACKEND=s3) ---
boto3==1.35.36

# --- Testing ---
pytest==8.3.3
httpx==0.27.2